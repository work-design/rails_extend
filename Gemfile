source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }
gemspec

gem 'rails'
gem 'puma'
gem 'pg'

gem 'amazing_print'

group :development, :test do
  gem 'debug', '>= 1.0.0.rc'
  gem 'sdoc'
end
